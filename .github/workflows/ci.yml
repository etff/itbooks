name: React build
on: 
  push:
    branches: [ master ]
  pull_request:
    branches: [ develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: npm install
        run: npm install

      - name: Build                   
        run: npm run build